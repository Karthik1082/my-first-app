name: My First Pipeline

on:
  push:
    branches:
      - main
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Step 1 - Get my code
      uses: actions/checkout@v3

    - name: Step 2 - Show files
      run: ls -la

    - name: Step3 - Print message
      run: echo "Hello Karthik! Pipeline is working"

    - name: Step 4 - Show date and time
      run: date
